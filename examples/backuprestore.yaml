apiVersion: astra.netapp.io/v1
kind: BackupRestore
metadata:
  name: test-backup-restore1
spec:   
  appVaultRef: test-appvault1
  appArchivePath: toBeReplaced
  namespaceMapping:
  - source: sourcens
    target: targetns